@page "/Main"
@using Mgt.Lit.WebFront.Components.Layout.MainLay
@layout AppLayout

<div class="d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="text-center">
        @* ปรับขนาด width ตามความเหมาะสมครับ *@
        <img src="/images/mgt-logo.png" alt="MGT Logo" style="width: 300px; opacity: 0.8;" />

        @* ถ้าอยากใส่ข้อความใต้รูปก็เปิดคอมเมนต์ด้านล่างนี้ครับ *@
        @* <h3 class="text-muted mt-4">Welcome to Mgt Lit System</h3> *@
    </div>
</div>

@code {
    // โค้ดส่วนอื่น ๆ (ถ้ามี)
}